import {} from "./configure";

describe("configureLoad", () => {
  it("should return a function", () => {
    // const result = configureLoad();
    // expect(result).toBeInstanceOf(Function);
  });
});
